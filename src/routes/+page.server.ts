// import type { PageServerLoad } from './$types';
// import { serverClient } from '../trpc/serverClient';

// export const load = (async (event) => {
// 	//console.log(await serverClient(event).hello());
// 	return {};
// }) satisfies PageServerLoad;
